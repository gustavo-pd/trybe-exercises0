//CALCULANDO ÁREA DE UM RETÂNGULO BASE = 5, ALTURA = 8;

//declarando as variáveis
let area = 0;
let perimetro = 0;

let base = 5;
let altura = 8;

area = base * altura;
perimetro = (2 * base) + (2 * altura);

console.log("valor da área do retângulo = " + area);
console.log("valor do perímetro do retângulo = " + perimetro);